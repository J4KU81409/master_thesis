The main file that runs the simulation is run_simulation.py. When run, the log files are created that document what is happening. The folder results contains the resulting dataframes of the experiments. 
